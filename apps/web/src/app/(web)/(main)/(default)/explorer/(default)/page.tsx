'use server'

import { Fragment } from "react";

const ExplorerRoutePage =  async () => {
    // const baseUrl = await CrepenHttpService.getUrl();
    // const pathname = new URL(baseUrl!).pathname;
    // redirect(`${pathname!}/folder`)

    return (<Fragment />);
    // return (
    //     <Fragment>
    //         {`${pathname!}/folder`}
    //     </Fragment>
    // )
}

export default ExplorerRoutePage;